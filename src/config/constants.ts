/**
 * Environments that we are checking at the app
 */
const ENVIRONMENTS = {
  PROD: 'production',
  DEV: 'development',
}

/**
 * Session storage keys
 */

const MSAL_TOKEN = 'msalToken'

export { ENVIRONMENTS, MSAL_TOKEN }
